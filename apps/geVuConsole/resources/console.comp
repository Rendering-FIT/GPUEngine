#version 430
layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 0) readonly buffer readBuffer{
    int inVertices[];
};

void main(){
	uint id = gl_GlobalInvocationID.x;

  inVertices[id] = 100 + id;
}
