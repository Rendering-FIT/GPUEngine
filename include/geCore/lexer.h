#pragma once

#include<vector>
#include<geCore/token.h>

namespace ge{
  namespace core{

    /*
    class Lexer{
      public:
        Lexer();
        ~Lexer();
        void parse(std::string data,bool stop = false);
        Token get();
        void reset();
      protected:
        bool _firstRun = true;
        std::vector<Token>_tokens;
        decltype(_tokens)::size_type _currentToken = 0;
    };
    */
  }
}
